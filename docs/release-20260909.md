# 商务系统正式发布 · 2026-09-09

状态：2026-09-09 15:00（服务器时间）正式部署完成。保持内网 HTTP；iOS 不在范围内。

## 访问与版本

- Web：http://192.168.0.253:8088/ 。已发布六阶段修复及最新手机/登录布局兼容修复。
- 安卓下载：http://192.168.0.253:8088/downloads/ 。0.2.5 / versionCode 7；可同签名覆盖正式旧版及 versionCode 6 预览包。
- APK：public/downloads/department-steward-0.2.5.apk，4,291,362 字节，SHA256 78FD468960A264B4F6BC580707B20BC316DA03A8A22AF979ED496542FB191D47。
- 同步更新 android.json；APP 打开/回前台自动检查，有新版提示，安装仍由用户确认。未通过 ADB 替用户安装本次正式包。
- Web/API 包内版本字符串仍为 0.2.0（该基础版本的修复发布），以本发布记录和镜像哈希识别此次部署；安卓独立版本为 0.2.5。

## 备份和演练

- 正式备份：D:\BusinessManagementApp\backups\preparation\20260909-143000，包括数据库、附件归档、源码、受限权限配置及运行镜像，7 个文件哈希核验通过。
- 数据库备份 SHA256：5ABF5382A7A417014B985FB610F1E9BE8D06074E136DD7191118354F7EDA9AD0；66,476 字节。
- 演练库：business_releasecheck_20260909_143000（仅商务数据库容器内，保留供追溯）。已从本次备份恢复并执行 001–005 迁移，不覆盖活动业务库。
- 迁移前、演练后及正式部署后数量一致：users 2、organizations 8、contacts 8、business_items 14、attachments 1。备份时附件存储归档无文件；该计数含历史元数据，不表示已验证每条历史附件可下载。
- 本地发布前快照：.preparation/20260909-145725，284 个文件。

## 构建与部署

- 本机 npm run verify 通过（28 后端 + 25 客户端，共 53 项）。最终 WebView 99 媒体查询兼容校验、安卓签名/包名/版本/实际地址/无嵌套包校验通过。
- 服务器正常源码构建在 npm ci 出现 ETIMEDOUT，未切换任何服务。改用本机已测试 Web/API 编译产物打包；依赖锁 SHA256 5C1F021A06CB95BF65DDAA6746D78111EACC33D93538C9DABC2375F3AB1D2FD4，与已有 QA 运行环境一致。
- API 运行环境复用已验证 QA 基础镜像 sha256:532fe5f6c4458acba7abbcd4da97ec172b2c0135a2301e4215b10998d39bd333，覆盖最新编译产物及迁移；正式环境仍由正式 Compose 注入，不使用 QA 数据库或凭据。
- 正式 API 镜像：business-management-api:release-20260909-143000，manifest sha256:4fdd808ad5052ceef1b332465fe42b4379a9a7d52ff35066f25d54080a24c76c。
- 正式 Web 镜像：business-management-web:release-20260909-143000，manifest sha256:76c30b83c8a23aa4a44d22d8618f6a78822ef04bb0e02726910ef7facda347ae。
- 源码与发布证据：D:\BusinessManagementApp\releases\release-20260909-143000。已将已发布源码同步回 D:\BusinessManagementApp\app，并更新常规 :0.2.0 镜像别名；生产 .env 和 Compose 配置未替换。
- 只重建 business-management 的 api/web；数据库、存储、备份服务和 QA 均未重建。切换期间有短暂 502，随后恢复。
- 备份后的保护基线覆盖 12 个容器，部署后 ID、镜像、启动/重启及端口一致，包括全部检测服务。检测前端在本次发布之前已有其他更新，比较使用本次基线，不使用昨日旧基线。

## 上线验收

- API health/ready：数据库、存储均 ready；Web/API 容器 healthy。
- 正式数据库迁移 001–005 均已登记，组织约束检查通过。
- 通知接口已发布：匿名 GET /api/v1/notifications 返回 401（不再是 Cannot GET 404），APP Origin 的 OPTIONS 返回 204、允许 https://localhost 和 Authorization 请求头。本次未冒充用户验证已登录的通知已读操作；此前 QA 功能矩阵已通过。
- APK 经实际 HTTP 下载后 SHA256 与元数据匹配。
- tests/production-release-smoke.cjs：360/1440 正式 Web 登录页、无横向溢出、0.2.5 下载链接和页面错误检查通过，没有登录或新增正式业务数据。
- 服务器最终证据：release-verification.json、protected-before.txt、protected-after.txt（位于发布目录）。没有 Git 暂存、提交、推送。

## 回滚与后续

- 应用回滚标签：business-management-api:pre-20260909-143000、business-management-web:pre-20260909-143000。发布目录 rollback.override.yaml 可与原 Compose 组合，仅对 api/web 执行 up --no-deps --no-build。
- 数据库不自动回灌旧备份，避免覆盖发布后的业务数据；本轮新增表可保留供旧应用回滚，组织约束迁移需单独评估后再撤销。
- 如果以后用普通 Compose 命令运维，以 D:\BusinessManagementApp\app、正式 .env、项目名 business-management 为准；不要对检测 Compose 或整个 Docker 引擎操作。
- 有限保留的演练库可在后续确认后删除；本次未删除。Windows/VPN 真机兼容性及 Office 实机待验事项仍按验收清单保留。
- 构建依赖安装输出 3 项 high 级审计告警，未在发布期间擅自升级依赖或执行 audit fix --force；须另行分析受影响路径与修复版本。此次没有做依赖安全专项验收。
