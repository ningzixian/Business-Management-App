# v0.2.0 验收记录

验收日期：2026-09-06

部署地址：`http://192.168.0.253:8088`

部署项目：`business-management`

## 代码与构建

- `npm run verify` 通过：Web 类型检查、Web 生产构建、NestJS 构建和 11 项 API 测试全部成功。
- Web 与 API 的 `npm audit --omit=dev` 均为 0 个已知漏洞。
- 完整前端依赖审计仍有 3 个仅开发期的中等级告警，来自 Capacitor CLI 的 Xcode 工具链，不进入 Nginx 运行镜像，当前上游无可用修复。
- Android Capacitor 同步通过，Debug APK 构建成功。
- APK：`android/app/build/outputs/apk/debug/app-debug.apk`
- APK SHA-256：`610BB7162F7F44997E17E74F03096E6C84150AA5FBBA3EA2C95773EF646C0DF2`
- iOS 工程已同步；模拟、签名和打包仍需 macOS 与 Xcode。

## 界面验收

- Web 登录与注册切换正常。
- 390px 手机视口下注册表单、账号中心和导航布局正常。
- Web 账号设置页可按当前用户显示姓名、用户名和角色。
- 修复 Web 首页日期格式在部分浏览器导致白屏的问题。

## 账号与权限

- 自助注册固定授予 `member`，注册者不能选择或提升角色。
- 用户主动修改密码后，变更前的访问令牌立即失效。
- 管理员调整角色后，变更前的访问令牌立即失效。
- 管理员重置密码、停用账号和停用后的登录拒绝均通过。
- 临时验收账号、刷新令牌及对应测试审计记录已清理，用户数恢复为验收前的 1。

## 服务器与数据

- Web、API、PostgreSQL、MinIO、数据库备份和附件备份 6 个容器运行正常。
- 仅 Web 发布宿主机端口 `8088`；API、数据库与对象存储不发布宿主机端口。
- 数据库已应用 `001_v0.2.0_core.sql` 与 `002_v0.2.0_account_security.sql`。
- 现存有效数据：1 个用户、8 个组织、8 个人脉、8 条任职关系、14 个事项、0 个有效附件。
- 最终数据库备份：`D:\BusinessManagementApp\backups\db\business_management_v0.2.0_final_20260906T135247.dump`
- 备份 SHA-256：`FB851D5B2685C1BDFB8C7E663F2C75035C8C0CEB4F5A13C343FC339DAE99A209`
- 备份已恢复到临时数据库并核对迁移与业务数量；临时数据库已删除。
- 定时数据库备份和附件快照容器持续运行，最近任务均显示完成。

## 隔离确认

- `cp-data-system-frontend`、`cp-data-system-backend`、`postgis-prod`、`mysql-prod` 状态与端口均保持不变。
- 上述 4 个检测业务容器最终重启次数均为 0。
- 本次只重建并切换 `business-management-api-1` 与 `business-management-web-1`；商务数据库、对象存储及检测业务容器未重建。

## 上公网前置项

- 关闭 `SELF_REGISTRATION_ENABLED`，或先升级为邀请码/统一身份认证。
- 配置 HTTPS，移除 Android 明文流量及 iOS ATS 临时放行。
- 完成 iOS 真机签名、移动端通知权限和公网安全测试。
