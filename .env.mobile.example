# 复制为 .env.mobile.local，并按实际内网地址修改。
# 这是手机 API 与原生更新的唯一服务来源。构建会生成 service-config.json，禁止手改原生常量。
# 公网必须 HTTPS；私网 HTTP 仅用于可信内网。模拟器地址 10.0.2.2 不得进入正式分发包。
VITE_API_BASE_URL=http://192.168.0.253:8088/api/v1
VITE_DEMO_MODE=false
